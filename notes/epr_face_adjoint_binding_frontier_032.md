# Face-adjoint binding frontier 032

Program 01 was queried as a canonical assembly interface.

It supplies:

- the native face neutral axis T = A1 - A0;
- exact descent of the projective T axis through D5 realization gauge;
- exact descent through the residual S1 connection-sheet ambiguity;
- native projective transport under the common order-four exchange g;
- historical r1 covariance of the oriented T-axis field;
- the three native face SU2-type axes K1,K2,K3.

Program 01 does not serialize:

- one concrete common adjoint coordinate frame for all six faces;
- one unique raw Hilbert transport matrix;
- six numerical/vector T-axis representatives in one common R3 frame;
- a native elementwise identification of those face axes with the
  analyzer six-line system.

Therefore the six-face projective Gram matrix cannot be reconstructed
from the Program-01 assembly interface without adding an adjoint
trivialization that Program 01 deliberately did not earn.

No further Program-01 or Project-41 archaeology is planned for this
interface.

The remaining local bridge is carried explicitly as a conditional
common-adjoint isometry

    Phi:
        analyzer 3-space
        ->
        face su(2) adjoint.

This is one common frame identification, not six independently fitted
setting directions.

Under that conditional interface the six sharp face observables are

    N_i^2 = I

and

    P_i = (I + N_i)/2.

Audit 027 did not natively construct P_i. It assumed the rank-one
face-qubit projectors and applied the coherent scalar response by
functional calculus:

    E_(i,r)
      =
    alpha_r P_i
      +
    beta_r (I-P_i).

Audits 028-030 therefore obstruct the inherited coherent occupation
pointer, not the existence of a functional sharp face axis.

Current EPR assembly state:

    G1800 joint preparation domain:
        available

    zero/nonzero wedge classifier:
        available

    canonical nonzero EPR line:
        available

    six native analyzer geometry:
        available

    Bell-capable native setting quartets:
        available

    native setting-to-face relation:
        available

    analyzer sector character:
        chi_deck

    functional local face C2:
        available

    native six-line analyzer-to-face adjoint binding:
        open

    conditional common-adjoint sharp face interface:
        available as an explicit hypothesis

    native joint receipt weighting/frequency law:
        open

Next target:

    derive or reject a native joint weighting law on the complete
    G1800 preparation domain, retaining zero/nonzero branches and all
    local receipt outcomes, then test normalization, no-signaling,
    CHSH, and Bell-local factorization.

